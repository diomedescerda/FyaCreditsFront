import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fya.credits',
  appName: 'Fya Credits',
  webDir: 'dist/fya-credits-front/browser',
  deepLinkScheme: 'com.fya.credits'
};

export default config;
